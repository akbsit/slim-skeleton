<!DOCTYPE html>
<html lang="{{ local }}">
<head>
    <meta charset="{{ charset }}" />
    <title>{{ name }}</title>
</head>
<body>
    <p>
        {{ name }} init success!
    </p>
    {{ csrf.field | raw }}
</body>
</html>